// The site is dark-only, so diagrams should be too. On GitHub the same fenced
// blocks are themed by GitHub instead, which is why the diagram source carries
// no colours of its own.
{
  theme: 'dark',
  themeVariables: {
    fontFamily: 'ui-sans-serif, system-ui, -apple-system, sans-serif',
    fontSize: '14px',
  },
  flowchart: {
    curve: 'basis',
    padding: 16,
    nodeSpacing: 40,
    rankSpacing: 55,
  },
}
